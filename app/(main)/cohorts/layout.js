export const metadata = {
  title: "Cohorts | HCL Academy",
  description:
    "Explore graduate projects from HCL Academy cohorts. See what students build in web development, design, and AI programs.",
};

export default function CohortsLayout({ children }) {
  return children;
}
