export const metadata = {
  title: "Admin Login | HCL Academy",
  robots: "noindex, nofollow",
};

export default function AdminLoginLayout({ children }) {
  return <div className="min-h-screen bg-white">{children}</div>;
}
